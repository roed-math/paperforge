---
kind: instruct          # instruct = write this | verbatim = insert my text as-is
target: sec-background   # optional xml:id to place near; omit to let Claude place
section: background      # optional human hint
---
Add a paragraph to the background section recalling the classification of the
tamely ramified quotient of the absolute Galois group, citing [Iwasawa]. Keep it
to the default detail level; put any proof sketch in a detail-level-2 block.
